import React, { Component } from "react";
import { Heading } from "grommet";

class Navbar extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <Heading>This is the Navbar</Heading>;
  }
}

export default Navbar;
